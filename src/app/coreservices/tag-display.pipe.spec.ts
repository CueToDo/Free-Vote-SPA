import { TagDisplayPipe } from './tag-display.pipe';

describe('TagDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new TagDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
